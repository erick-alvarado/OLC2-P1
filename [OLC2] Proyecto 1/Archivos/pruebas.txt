program factorial;

function fact(n: integer): real;
begin
    if (n = 0) then
    begin
        fact := 1
    end;
    else
    begin
        fact := n * fact(n - 1);
    end;
end;

var
    n: integer;

begin
    for n := 0 to 2 do
    begin
        writeln(n, '! = ', fact(n));
    end;
end.